name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          set -e
          cd /var/www/us-market-hub
          git pull origin main
          npm ci --legacy-peer-deps
          npm run build
          pm2 stop us-market-hub || true
          pm2 start us-market-hub
          echo "Deploy OK: $(date)" && pm2 list
